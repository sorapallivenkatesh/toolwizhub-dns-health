{
  "name": "toolwizhub-dns-health",
  "version": "0.1.0",
  "private": true,
  "description": "Run the DNS & Cert Health API and static site locally.",
  "scripts": {
    "api": "node api/src/dev.js",
    "site": "python3 -m http.server 8080 --directory site",
    "test": "node api/tests/check.test.js",
    "dev": "trap 'kill 0' SIGINT EXIT; node api/src/dev.js & python3 -m http.server 8080 --directory site"
  }
}
